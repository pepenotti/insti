name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore src\Insti.API.Test
    - name: Build
      run: dotnet build src\Insti.API.Test --no-restore
    - name: Test
      run: dotnet test src\Insti.API.Test --no-build --verbosity normal
